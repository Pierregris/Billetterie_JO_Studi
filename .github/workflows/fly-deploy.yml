name: Fly Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    concurrency: deploy-group
    steps:
      - uses: actions/checkout@v4

      # Déployer avec Fly action officielle
      - uses: superfly/flyctl-actions/deploy@v1
        with:
          token: ${{ secrets.FLY_API_TOKEN }}
          remote-only: true